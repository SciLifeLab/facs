language: python

before_install:
    - sudo apt-get install -y build-essential zlib1g-dev zlib1g libgomp1
    - gcc -v
    - lsb_release -a
python:
    - 2.7
script:
    - make tests
notifications:
  email: false
